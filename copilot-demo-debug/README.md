# Buggy Expense Tracker

A tiny CLI app to add/list expenses and store them in a JSON file.

This project is intentionally buggy for debugging practice with GitHub Copilot.

## Run
python -m expense_tracker.app

## Run tests
pytest -q
